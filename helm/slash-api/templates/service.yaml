apiVersion: v1
kind: Service
metadata:
  name: {{ include "slash-api.fullname" . }}
  labels:
    {{- include "slash-api.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "slash-api.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
