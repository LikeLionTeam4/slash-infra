apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "slash-nlu.fullname" . }}
  labels:
    {{- include "slash-nlu.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "slash-nlu.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "slash-nlu.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "slash-nlu.fullname" . }}
      containers:
        - name: slash-nlu
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          {{- with .Values.env }}
          env:
            {{- range $k, $v := . }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
