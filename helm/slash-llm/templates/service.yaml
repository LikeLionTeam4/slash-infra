apiVersion: v1
kind: Service
metadata:
  name: {{ include "slash-llm.fullname" . }}
  labels:
    {{- include "slash-llm.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "slash-llm.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
